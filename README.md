 // // Get all tickets provided by the user
        // $user = User::find(1);
        // $providedTickets = $user->providedTickets;

        // // Get all tickets where the user is a tenant
        // $tenantTickets = $user->tenantTickets;